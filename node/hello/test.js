var rls = require('readline-sync');

var val1=10

var val2=20

var sum = val1 + val2

console.log(sum)

var master = rls.question('who is the cause of the universe , himself without a cause ? :');

console.log('cause of all is '+master +'!' );

